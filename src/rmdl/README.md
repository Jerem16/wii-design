# RMDL — Engine (Option A)

Dossier moteur (lexer/parser/validate/sanitize + génération TSX).

- Le TSX généré doit rester 100% React (pas de HTML brut).
- Les tags RMDL sont en minuscules.
- Les fermetures se font par répétition du tag (pas de slash).
