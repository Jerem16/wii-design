# RMDL — Sources

- `pages/*.rmdl` : sources éditoriales (format RMDL).
- `glossary.json` : définitions/infobulles partagées (optionnel).

Rappel : les tags RMDL sont en minuscules et se ferment par répétition du tag (pas de slash).
